# ============================================================
# Project Echo — Python FastAPI 数据面依赖
# 所有模型和服务均在本地运行，无第三方云服务依赖
# ============================================================

# ── Web 框架 ────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ── 配置管理 ─────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0
python-dotenv==1.0.1

# ── Redis 客户端（任务队列通信）──────────────────────────────
redis==5.0.4

# ── MinIO 本地对象存储 ────────────────────────────────────────
minio==7.2.7

# ── Ollama 本地 LLM 客户端（Qwen2.5 摘要/理解）──────────────
ollama==0.2.1

# ── WhisperX 本地语音转写 ─────────────────────────────────────
# PyAV 须 >=13 以使用预编译 wheel，避免与 Debian/FFmpeg 7 源码编译不兼容
# whisperx 3.1.x 锁定 faster-whisper==1.0.1（要求 av==11.*），与 av>=13 冲突
av>=13.0.0,<16.0.0
faster-whisper>=1.2.1
whisperx==3.4.2

# ── 本地向量嵌入模型 ──────────────────────────────────────────
sentence-transformers==3.0.1

# ── HTTP 客户端（回调控制面用）────────────────────────────────
httpx==0.27.0

# ── 加密工具（AES-256-GCM）────────────────────────────────────
cryptography>=42.0.0

# ── 工具库 ─────────────────────────────────────────────────
python-multipart==0.0.9
aiofiles==23.2.1
